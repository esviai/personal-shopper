<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/header.ejs %>
    <title>Add item | Personal Shopper</title>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col s12">
          <h1 class="center">Personal Shopper</h1>
        </div>
      </div>
    </div>
    <div class="container">
      <div class="row">
        <div class="col s6 offset-s3 center">
          <form action="" method="post">
            <input type="text" name="image" value="<%- item.image %>" placeholder="<%- item.image %>" required>
            <br/>
            <input type="text" name="name" value="<%- item.name %>" placeholder="<%- (item.name) ? item.name : `Name` %>">
            <br/>
            <input type="text" name="category" value="<%- item.category %>" placeholder="<%- (item.category) ? item.category: `Category` %>">
            <br/>
            <input type="number" min=0 name="price" value="<%- item.price %>" placeholder="<%- (item.price) ? item.price : `Price` %>""Price">
            <br/>
            <input type="number" min=1 name="stock" value="<%- item.stock %>" placeholder="<%- (item.stock) ? item.stock : `Stock` %>""Stock">
            <br/>
            <input type="text" name="description" value="<%- item.description %>" placeholder="<%- (item.description) ? item.description : `Description`  %>""Description">
            <br/>
            <button type="submit" class="btn-large">Submit</button>
          </form>
        </div>
      </div>
    </div>
    <% include ./partials/footer.ejs %>
  </body>
</html>
